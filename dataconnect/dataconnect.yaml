specVersion: "v1beta"
serviceId: "medicaction"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "medicaction-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
